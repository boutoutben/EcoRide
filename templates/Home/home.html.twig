{% extends "base.html.twig" %}
{% block javascripts %}
	{{ parent() }}
	{{ encore_entry_script_tags('homeRedirection') }}
{% endblock %}
    
{% block body %}
    <main>
        <section class='block' id="presentation">
            <div>
                <h1>Présentation d'EcoRide</h1>
                <p><img src='img/presentationEcoRide.jpg'alt="présentation d'EcoRide">EcoRide est une plateforme de covoiturage pensée pour simplifier les trajets partagés tout en réduisant l'impact environnemental des déplacements. Ce site vise à connecter des conducteurs avec des passagers partageant le même itinéraire, permettant à chacun de voyager de manière plus économique, conviviale,  et durable. Voici les principaux aspects d'EcoRide.</p>           
                <div class='clear'></div>
                <button id="fullPresentationBtn" class='standard-btn'>En savoir plus</button>
            </div>
            <div>
                <h1>Rejoindre l'équipe d'EcoRide</h1>
                <p><img src="img/recrutment.jpg" alt="personne travaillant chez EcoRide">Vous êtes passionné(e) par l'innovation, la mobilité durable, et souhaitez participer activement à la réduction de l'empreinte carbone ? EcoRide est à la recherche de talents engagés pour développer notre plateforme de covoiturage écoresponsable !</p>
                <div class='clear'></div>
                <button id="recrutmentBtn" class='standard-btn'>Nous rejoindre</button>
            </div>
        </section>
        <section class='block' id='home-search-bar'>
            <h1>Itinéraire</h1>
            <div class="allInput">
                {{ form_start(form) }}
                    <div class="inputBackground search">
                        <img src="img/loupe.png" alt='loupe'/>
                        {{ form_widget(form.startPlace) }} 
                    </div>
                    {% for error in form.startPlace.vars.errors %}
                            <div class="form_error">{{ error.message }}</div>
                    {% endfor %}
                    <div class="inputBackground search">
                        <img src="img/loupe.png" alt='loupe'/>
                        {{ form_widget(form.endPlace) }} 
                    </div>
                    {% for error in form.endPlace.vars.errors %}
                            <div class="form_error">{{ error.message }}</div>
                    {% endfor %}
                    <div class="inputBackground date none">
                        <img src="img/calendar.png" alt='canlendrier'/>
                        {{ form_widget(form.startDate) }}
                    </div>
                    {% for error in form.startDate.vars.errors %}
                            <div class="form_error">{{ error.message }}</div>
                    {% endfor %}
                </div>    
                {{form_row(form.submit)}}         
            {{ form_end(form) }}          
        </section>
    </main>
{% endblock body %}